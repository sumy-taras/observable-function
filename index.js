'use strict';

const { wrap } = require('./es6/wrap')
const { stub, transfer, holder, transferr, holderr, casts } = require('./es6/handy')

module.exports = {
  wrap,

  stub,
  transfer,
  holder,
  transferr,
  holderr,
  casts
}
